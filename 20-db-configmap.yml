apiVersion: v1
kind: ConfigMap
metadata:
  name: db-config-b
  namespace: apps
data:
  DB_HOST: "pg-db-b.apps.svc.cluster.local."  # trailing dot avoids search retries
  DB_PORT: "5432"
  DB_NAME: "appdb"
  VAULT_DB_ROLE: "app-db-role-b"